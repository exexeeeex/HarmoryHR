import { AuthenticationCard } from '@/shared/ui/cards';

const Page = () => {
    return (
        <main className='w-full h-screen flex justify-center items-center flex-col'>
            <AuthenticationCard />
        </main>
    );
};

export default Page;
